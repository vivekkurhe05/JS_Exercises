/**
 * Write a Python program to replace whitespaces with an underscore and vice versa.
 */

// my solution
let text = 'Python Exercises'

console.log(text.replace(/\s/g, "_"))
console.log(text.replace(/_/g, " "));