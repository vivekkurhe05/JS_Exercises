/**
 * Number.isNaN() method checks if the value is NaN. and the type is number
 * 
 * To guess the result, use this approach
 * 
 * typeof (value) -> should be number
 * isNaN(value) -> should be NaN
 * 
 * if the above two conditions is met, it returns true.
 */